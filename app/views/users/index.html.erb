<div class="container main-container">
  <div class="row justify-content-center">
    <div class="col-sm-12 col-md-10 col-lg-8 items-container">
        <% @users.each do |user| %>
          <div class="index-user">
            <%= link_to user_path(user) do %>
              <h3><%= user.name %></h3>
            <% end %>
            <h5><%= user.tasks.count %></h5>
          </div>
        <% end %>
    </div>
  </div>
</div>
